import * as React from "react";
import { Table } from "@chakra-ui/react";
import type { TableProps } from "@chakra-ui/react";

export const OurTable = (props: TableProps) => {
  return <Table {...props} />;
};
